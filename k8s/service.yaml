apiVersion: v1
kind: Service
metadata:
  name: github-gists-api-service
  namespace: default
spec:
  selector:
    app: github-gists-api
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: LoadBalancer
